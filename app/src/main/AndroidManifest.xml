<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.kevin.cn.ks">

    <uses-permission android:name="android.permission.SET_DEBUG_APP" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="cn.ks.activity.DailyCheck">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="cn.ks.activity.ManageVpsActivity" />
        <activity android:name="cn.ks.activity.BaseActivity" />
        <activity android:name="cn.ks.activity.CalendarActivity" />
        <activity android:name="cn.ks.activity.StatisticsActivity" />
        <activity android:name="cn.ks.activity.SettingActivity" />
        <activity android:name="cn.ks.activity.VpsDetailActivity" />
    </application>

</manifest>